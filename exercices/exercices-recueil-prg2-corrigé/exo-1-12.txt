scanf

Le code ci-dessous n'est pas correct. Le récrire complètement de sorte à corriger
toutes les erreurs / maladresses commises.

#include <cstdlib>
#include <cstdio>

using namespace std;

int main() {
 int n, char c;
 printf("Donnez un nombre entier et un caractere : ");
 scanf("%d%c", n, c);
 printf("n = %d, c = %c\n", n, c);
 return EXIT_SUCCESS;
}